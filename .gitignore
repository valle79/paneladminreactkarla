# Node
node_modules/
dist/
*.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Entorno / secretos
.env
.env.local

# VITE_API_URL es pública (viaja en el bundle del cliente); se versiona como respaldo
!.env.production

# Archivos subidos localmente (se almacenan en Cloudinary en producción)
backend/uploads/*
!backend/uploads/.gitkeep

# Respaldo local de la BD (contiene datos y podría incluir info sensible)
database/backups/

# Editor / SO
.vscode/
.DS_Store
Thumbs.db